<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="coordination">

    <!-- 通过主键查找 -->
    <select id="getCoordinationById" parameterType="integer">
        select *
        from coordination
        where id = #{id}
    </select>
    <insert id="saveCoorination" keyProperty="id" useGeneratedKeys="true" parameterType="coordination">
        insert into coordination (space_id, file_id)
        values (#{spaceId}, #{fileId})
    </insert>
</mapper>

